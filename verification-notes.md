# ملاحظات التحقق المرحلي

- `pnpm lint` نجح دون أخطاء TypeScript.
- `pnpm build` نجح بعد ضبط `workbox.maximumFileSizeToCacheInBytes` إلى 4 MiB.
- تحذير Vite المتبقي متعلق بحجم chunk يتجاوز 500 kB، لكنه لا يمنع البناء أو إنشاء ملفات PWA.
- صفحة `/login` تعرض أزرار الحسابات التجريبية الأربعة.
- زر مدير المدرسة التجريبي نقل الجلسة إلى `/principal` وظهرت لوحة المدير العربية، مع بيانات المدرسة التجريبية وعبارة تعذر قراءة إعدادات المدرسة بسبب عدم اتصال Firestore/عدم توفر بيانات حقيقية.
- لم تظهر أخطاء JavaScript في سجل المتصفح أثناء فتح صفحة الدخول.

## قيد معروف
الحسابات التجريبية الحالية جلسات معاينة محلية للاختبار وليست حسابات Firebase حقيقية؛ لذلك لا تفترض وجود مستخدمين فعليين في قاعدة البيانات إلى أن تُضاف بيانات Firebase/حسابات مصادق عليها.

- بعد تسجيل الخروج، زر المعلمة التجريبي نقل الجلسة إلى `/teacher` وظهرت لوحة المعلمة باسم سمية مع تبويبات الفصول والطلاب والاختبارات والتنبيهات والتقويم والمحادثات.

- بعد تسجيل الخروج مرة أخرى، زر الطالب التجريبي نقل الجلسة إلى `/student` وظهرت لوحة الطالب باسم محمد، ومكتبة تفاعلية تعرض 9 مواد مع روابط المناهج والاختبارات والمسابقات والملف الشخصي والمحادثات.

## التحقق النهائي المرحلي — 2026-08-24
- `pnpm lint` و`pnpm build` نجحا بعد ربط فصول المعلم بـ Firestore وإكمال الوحدات الأكاديمية.
- إنشاء PWA precache نجح بحجم يقارب 2.6 MiB بعد رفع حد Workbox؛ تحذير حجم chunk ما زال غير مانع للبناء.
- بوابة ولي الأمر التجريبية تعرض ابنًا مترابطًا مع معدل 86% وحضور 18/20 واختبارات ومسابقات.
- تم إصلاح سباق ترطيب Zustand الذي كان يسبب استعلام Firebase مبكرًا وخطأ الصلاحيات للحساب التجريبي.
- سجل المتصفح بعد إعادة التحميل لم يعد يحتوي على خطأ الصلاحيات.
- لا تزال اختبارات Firebase الحقيقية، إدخال المناهج المعتمدة كاملة، واختبار العمل دون اتصال على جهاز فعلي بحاجة إلى إعداد أو تحقق خارجي.
